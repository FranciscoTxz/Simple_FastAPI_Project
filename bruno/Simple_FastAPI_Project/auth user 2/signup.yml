info:
  name: signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/auth/signup"
  body:
    type: json
    data: |-
      {
        "name": "Angel Torres 2",
        "password": "PassWord123@"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
