info:
  name: delete project
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{URL}}/project/2"
  headers:
    - name: authorization
      value: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
