info:
  name: get project
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/project/1/info"
  headers:
    - name: authorization
      value: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
