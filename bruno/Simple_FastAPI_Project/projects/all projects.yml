info:
  name: all projects
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/projects"
  headers:
    - name: authorization
      value: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
