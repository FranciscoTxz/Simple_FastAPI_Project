info:
  name: invite user to project
  type: http
  seq: 8

http:
  method: POST
  url: "{{URL}}/project/1/invite?user_email=ramonmtz@hot.com"
  headers:
    - name: authorization
      value: "{{TOKEN}}"
  params:
    - name: user_email
      value: ramonmtz@hot.com
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
