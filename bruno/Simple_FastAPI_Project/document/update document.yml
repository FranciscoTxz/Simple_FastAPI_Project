info:
  name: update document
  type: http
  seq: 2

http:
  method: PUT
  url: "{{URL}}/document/1"
  headers:
    - name: authorization
      value: "{{TOKEN}}"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - C:\Users\AngelTorres\Downloads\phrase_bank.pdf

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
