info:
  name: delete document
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{URL}}/document/2"
  headers:
    - name: authorization
      value: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
